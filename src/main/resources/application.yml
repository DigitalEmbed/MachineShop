server:
  error:
    include-stacktrace: on_param #To use stacktrace: http://ip:port/controller/patch/input?trace=true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cigarettedisplay?createDatabaseIfNotExist=true #jdbc:mysql://localhost:3306/anime?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

